
<html>
  <head>
    <% include ./partials/head %>
  </head>
  <body>
    <header>
      <% include ./partials/header %>
    </header>
    <div class="container">
      <% var url = "/hobbies/" + hobbies.id + "?_method=PUT" %>
      <form method="POST" action=<%= url %> >
        <input required name="title" value=<%= hobbies.title %> />
        <a href="/movies" class="btn red">Cancel</a>
        <button class="btn">Save</button>
     </form>
   </div>
  </body>
</html>